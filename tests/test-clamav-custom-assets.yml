name: Test ClamAV custom assets directory and ignores_post
on:
  workflow_dispatch:
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
      - name: Create custom assets directory
        run: mkdir -p /tmp/clamav-custom-assets
      - id: action
        uses: .
        with:
          clamav_enable: "True"
          clamav_update: "False"
          clamav_customassets_directory: "/tmp/clamav-custom-assets"
          clamav_customassets_use: ".+"
          yara_enable: "False"
          ignores_post: |
            $True
          found_log: "True"
          statistics_log: "True"
      - name: Assert finish output
        env:
          ACTUAL: ${{ steps.action.outputs.finish }}
        run: |
          echo "actual finish: $ACTUAL"
          if [ "$ACTUAL" != "True" ]; then
            echo "FAIL: expected finish to be 'True', got '$ACTUAL'"
            exit 1
          fi
          echo "PASS"
