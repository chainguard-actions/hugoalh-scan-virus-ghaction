# yaml-language-server: $schema=https://json.schemastore.org/github-workflow.json
name: "Sync Labels"
on:
  push:
    branches:
      - "main"
    paths:
      - ".github/labels.yml"
  workflow_dispatch:
permissions:
  issues: "write"
  pull-requests: "write"
jobs:
  main:
    name: "Main"
    uses: "hugoalh/hugoalh/.github/workflows/reusable-sync-labels.yml@bd6d88d955061b37108519fb4e595ed71403c0e3" # main
